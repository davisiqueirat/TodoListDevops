name: Deploy with Self-Hosted Runner

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
    - name: Deploy Application
      run: |
        echo "🚀 Deploy iniciado"
        cd /opt/deploy/TodoListDevops
        git pull
        docker compose down
        docker compose up -d --build
        docker ps
        echo "✅ Deploy concluído!"